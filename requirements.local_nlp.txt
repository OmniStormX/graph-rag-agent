-r requirements.txt

# 本地 NLP/向量模型扩展依赖。
# 这些依赖通常会间接带入 torch，不建议作为默认运行时依赖。
hanlp==2.1.1
sentence_transformers==4.1.0

# 如果后续需要 DeepSeek tokenizer 本地计数或训练链路，可再按需启用：
# transformers==4.49.0
# torch==2.5.1
# tiktoken==0.9.0

# 以下是 GRPO 训练所需的额外依赖，默认不安装：
# unsloth==2025.3.19
# unsloth_zoo==2025.3.17
# trl==0.14.0
# peft==0.15.1
# networkx==3.4.2
# vllm==0.6.5
# triton==3.1.0
# regex==2024.11.6
# tenacity==9.0.0
